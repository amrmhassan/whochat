import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
  root: {
    margin: '0 auto',
    fontWeight: 'bold',
    zIndex: '100',
    width: '100%',
  },
}));
export default useStyles;
